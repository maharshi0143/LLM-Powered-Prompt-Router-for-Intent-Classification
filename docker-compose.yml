version: "3.9"

services:
  ai-prompt-router:
    build: .
    container_name: ai-prompt-router
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped